# Automated PDF Contact Extractor 📄

## 🚀 Overview
A Python automation tool designed to streamline data entry. It scans a folder of PDF resumes, extracts **Email Addresses** and **Phone Numbers** using Regular Expressions (Regex), and saves them into a structured CSV file.

## 🛠️ Tech Stack
- **Language:** Python 3.x
- **Libraries:** `PyPDF2` (PDF Parsing), `re` (Pattern Matching), `csv` (Data Export)
- **Key Concepts:** Automation, File Handling, Data Parsing

## ⚙️ How to Run
1. Install dependencies:
   ```bash
   pip install PyPDF2
2. Place your PDF file in the same folder.
3. Run the script:
   ```bash
    python extractor.py
5. Check extracted_contacts_csv for the results.

<!-------end list------->
